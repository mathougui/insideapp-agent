language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt

script:
  - python -m pytest
  - sh build.sh

deploy:
  provider: releases
  api_key: $GITUHB_OAUTH_TOKEN
  file: "ia-agent"
  skip_cleanup: true
  on:
    tags: true
